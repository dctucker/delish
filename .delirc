ls = {
  local $pwd = Path.pwd
  for $f in $pwd.list {
    $stat = $f.stat
    $str = ""
    if $stat.mode > 040000 {
      $str += "d"
    } else {
      $str += "-"
    }
    out $str, " ", $f
    #.stat.mode.oct, " ", $f
  }
}
ls
